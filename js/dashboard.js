window.Dashboard={render(){let d=CC.data;let next=d.timetable[0];return `<section class="page"><h1 class="page-title">Good day, ${UI.esc(CC.user.name)}</h1><p class="page-sub">Everything important from your campus in one place.</p><div class="grid stats"><div class="card stat"><div class="small">Today's classes</div><div class="num">${d.timetable.length}</div><span class="badge">Schedule</span></div><div class="card stat"><div class="small">Unread notices</div><div class="num">${d.notices.length}</div><span class="badge important">Updates</span></div><div class="card stat"><div class="small">Saved materials</div><div class="num">${d.materials.filter(x=>x.saved).length}</div><span class="badge success">Library</span></div><div class="card stat"><div class="small">Notifications</div><div class="num">${d.notifications.filter(x=>!x.read).length}</div><span class="badge urgent">New</span></div></div><div class="grid two" style="margin-top:18px"><div class="card"><div class="row"><h2>Next class</h2><button class="btn secondary" onclick="Nav.go('timetable')">View timetable</button></div>${next?`<div class="item class"><b>${next.subject}</b><div class="small">${next.day} · ${next.time} · ${next.room}</div></div>`:UI.empty('No upcoming class')}</div><div class="card"><div class="row"><h2>Important notices</h2><button class="btn secondary" onclick="Nav.go('notices')">View all</button></div><div class="list">${d.notices.slice(0,3).map(n=>`<div class="item notice"><div class="row"><b>${UI.esc(n.title)}</b><span class="badge ${n.priority.toLowerCase()}">${n.priority}</span></div><div class="small">${UI.esc(n.body)}</div></div>`).join('')}</div></div></div></section>`}};
