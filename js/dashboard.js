import{state}from'./state.js';export function dashboard(){return `<div class="page-head"><div><h2>Welcome back 👋</h2><p class="muted">PAF-IAST • BS Computer Science • Semester 5</p></div><div class="actions"><button class="btn primary" data-go="notices">Create announcement</button></div></div><div class="grid stats"><div class="card stat"><span class="muted">Today's classes</span><b>${state.classes.filter(x=>x.day==='Mon').length}</b></div><div class="card stat"><span class="muted">Unread notices</span><b>${state.notifications.filter(x=>!x.read).length}</b></div><div class="card stat"><span class="muted">Materials saved</span><b>${state.materials.filter(x=>x.saved).length}</b></div><div class="card stat"><span class="muted">Active groups</span><b>${state.groups.length}</b></div></div><div class="grid two" style="margin-top:18px"><div class="card"><div class="section-title"><h2>Today's schedule</h2><button class="btn" data-go="timetable">Full timetable</button></div><div class="timeline">${state.classes.filter(x=>x.day==='Mon').map(x=>`<div class="class-row"><b>${x.time}</b><div><strong>${x.subject}</strong><div class="muted">${x.room}</div></div><span class="tag">Class</span></div>`).join('')}</div></div><div class="card"><div class="section-title"><h2>Official notices</h2><button class="btn" data-go="notices">View all</button></div>${state.notices.map(n=>`<div class="notice ${n.priority==='Urgent'?'urgent':n.priority==='Important'?'important':''}"><strong>${n.title}</strong><div class="muted">${n.body}</div></div>`).join('')}</div></div>`}
