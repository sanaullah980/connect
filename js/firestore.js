// Firestore CRUD is isolated here so UI can be edited independently.
export const paths={institution:i=>`institutions/${i}`,notices:i=>`institutions/${i}/announcements`,materials:i=>`institutions/${i}/materials`,groups:i=>`institutions/${i}/groups`};
