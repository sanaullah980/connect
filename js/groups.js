import{state}from'./state.js';import{modal,toast,esc}from'./ui.js';export function groups(){const g=state.groups[0];return `<div class="page-head"><div><h2>Subject Groups</h2><p class="muted">Official academic communication</p></div><button class="btn primary" id="new-group">New group</button></div><div class="grid two"><div class="card"><div class="item"><div class="avatar">💬</div><div><strong>${g.name}</strong><div><span class="tag success">Official</span></div></div></div><div class="muted">Pinned: Quiz topics and reading material</div></div><div class="card"><div class="section-title"><h2>${g.name}</h2><span class="tag">Live-ready</span></div><div class="chat">${g.messages.map(m=>`<div class="bubble ${m.me?'me':''}"><strong>${m.name}</strong><div>${esc(m.text)}</div></div>`).join('')}</div><div class="row" style="margin-top:12px"><input id="msg" class="input" style="flex:1" placeholder="Write a message"><button id="send-msg" class="btn primary">Send</button></div></div></div>`}export function bindGroups(){document.getElementById('send-msg')?.addEventListener('click',()=>{let i=document.getElementById('msg');if(!i.value.trim())return;state.groups[0].messages.push({name:'You',text:i.value,me:true});document.dispatchEvent(new Event('render'))});document.getElementById('new-group')?.addEventListener('click',()=>modal('Create group',`<div class="form"><label>Group name<input class="input"></label><button class="btn primary" data-close>Create</button></div>`,()=>toast('Group creation UI ready for Firestore'))}
