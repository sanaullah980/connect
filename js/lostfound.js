window.LostFound={render(){return `<section class="page"><div class="row"><div><h1 class="page-title">Lost & Found</h1><p class="page-sub">Help campus items find their owners.</p></div><button class="btn" onclick="LostFound.add()">+ Report item</button></div><div class="grid two">${CC.data.lostFound.map((x,i)=>`<div class="card"><div class="row"><span class="badge ${x.type==='Lost'?'urgent':'success'}">${x.type}</span><span class="small">${x.status}</span></div><h3>${UI.esc(x.title)}</h3><p class="small">📍 ${UI.esc(x.location)}</p><button class="btn secondary" onclick="LostFound.resolve(${i})">${x.status==='Active'?'Mark resolved':'Resolved'}</button></div>`).join('')||UI.empty('No items')}</div></section>`},add(){UI.modal('Report item',`<form onsubmit="LostFound.save(event)"><div class="field"><select id="ltype"><option>Lost</option><option>Found</option></select></div><div class="field"><input id="ltitle" required placeholder="Item description"></div><div class="field"><input id="lloc" required placeholder="Location"></div><button class="btn">Publish report</button></form>`)},save(e){e.preventDefault();CC.data.lostFound.unshift({type:ltype.value,title:ltitle.value,location:lloc.value,status:'Active'});CC.save();UI.close();App.render()},resolve(i){CC.data.lostFound[i].status='Resolved';CC.save();App.render()}};
