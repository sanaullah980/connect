window.App={page:'dashboard',menuOpen:false,can(r){return (CC.user.roles||[]).includes('institution_admin')||(CC.user.roles||[]).includes(r)},start(){authRoot.classList.add('hidden');appRoot.classList.remove('hidden');let items=Nav.items.filter(x=>x[0]!=='roles'||this.can('institution_admin'));appRoot.innerHTML=`<div class="shell"><aside class="sidebar"><div class="nav-brand">● CONNECT CAMPUS</div>${items.map(x=>`<button class="nav-item" data-page="${x[0]}" onclick="Nav.go('${x[0]}')">${x[1]} <span>${x[2]}</span></button>`).join('')}<button class="nav-item" onclick="Auth.logout()">↪ Sign out</button></aside><div class="drawer-backdrop" onclick="App.closeMenu()"></div><aside class="mobile-drawer"><div class="drawer-head"><b>CONNECT CAMPUS</b><button class="icon-btn" onclick="App.closeMenu()">✕</button></div>${items.map(x=>`<button class="nav-item" data-page="${x[0]}" onclick="Nav.go('${x[0]}');App.closeMenu()">${x[1]} <span>${x[2]}</span></button>`).join('')}<button class="nav-item" onclick="Auth.logout()">↪ Sign out</button></aside><main class="content"><header class="topbar"><div class="row"><button class="menu-btn" onclick="App.openMenu()">☰</button><div><b>${CC.user.name||'User'}</b><div class="small">${CC.data.profile.institution||'Connect Campus'}</div></div></div><div class="row"><button class="btn secondary" onclick="Nav.go('notifications')">🔔 <span id="unread"></span></button></div></header><div id="pageRoot"></div></main></div>`;this.render();if(FB.enabled&&FB.listenNotices){this.unsubNotice=FB.listenNotices(rows=>{CC.data.notices=rows;CC.save();if(this.page==='notices'||this.page==='dashboard')this.render()})}},openMenu(){appRoot.classList.add('menu-open')},closeMenu(){appRoot.classList.remove('menu-open')},render(){let f={dashboard:Dashboard,timetable:Timetable,notices:Notices,materials:Materials,groups:Groups,lostfound:LostFound,notifications:Notifications,profile:Profile,roles:Roles,settings:Settings}[this.page]||Dashboard;pageRoot.innerHTML=f.render();document.querySelectorAll('.nav-item').forEach(x=>x.classList.toggle('active',x.dataset.page===this.page));let n=(CC.data.notifications||[]).filter(x=>!x.read).length,u=document.getElementById('unread');if(u)u.textContent=n?'('+n+')':''}};Auth.init();
