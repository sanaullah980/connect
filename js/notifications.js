window.Notifications={render(){return `<section class="page"><div class="row"><div><h1 class="page-title">Notifications</h1><p class="page-sub">Your latest campus updates.</p></div><button class="btn secondary" onclick="Notifications.readAll()">Mark all read</button></div><div class="list">${CC.data.notifications.map((n,i)=>`<button class="item" style="text-align:left;${n.read?'opacity:.65':''}" onclick="Notifications.read(${i})"><b>${n.read?'':'● '}${UI.esc(n.text)}</b><div class="small">${n.read?'Read':'Unread'}</div></button>`).join('')||UI.empty('No notifications')}</div></section>`},read(i){CC.data.notifications[i].read=true;CC.save();App.render()},readAll(){CC.data.notifications.forEach(x=>x.read=true);CC.save();App.render()}};
