window.Profile={render(){let p=CC.data.profile;return `<section class="page"><h1 class="page-title">Academic Profile</h1><p class="page-sub">Your campus and academic information.</p><div class="card"><div class="row"><div><h2>${UI.esc(CC.user.name)}</h2><div class="small">${UI.esc(CC.user.email)}</div></div><button class="btn" onclick="Profile.edit()">Edit profile</button></div><table class="table"><tbody>${[['Institution',p.institution],['Department',p.department],['Program',p.program],['Semester',p.semester],['Section',p.section],['Term',p.term]].map(x=>`<tr><th>${x[0]}</th><td>${UI.esc(x[1])}</td></tr>`).join('')}</tbody></table></div></section>`},edit(){let p=CC.data.profile;UI.modal('Edit academic profile',`<form onsubmit="Profile.save(event)">${['institution','department','program','semester','section','term'].map(k=>`<div class="field"><label>${k}</label><input id="p_${k}" value="${UI.esc(p[k])}"></div>`).join('')}<button class="btn">Save profile</button></form>`)},save(e){e.preventDefault();Object.keys(CC.data.profile).forEach(k=>CC.data.profile[k]=document.getElementById('p_'+k).value);CC.save();UI.close();App.render();UI.toast('Profile updated')}};
