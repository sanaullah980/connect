window.Roles={available:['Student','Teacher','CR','Institution Admin','Announcement Publisher','Timetable Manager','Moderator'],render(){return `<section class="page"><h1 class="page-title">Roles</h1><p class="page-sub">Institution-level access and responsibilities.</p><div class="card"><div class="row"><h2>Assigned roles</h2><button class="btn" onclick="Roles.assign()">Assign role</button></div><div class="list">${CC.data.roles.map((r,i)=>`<div class="item row"><div><b>${UI.esc(r)}</b><div class="small">Institution scope</div></div>${r!=='Student'?`<button class="btn danger" onclick="Roles.remove(${i})">Remove</button>`:''}</div>`).join('')}</div></div></section>`},assign(){UI.modal('Assign role',`<div class="field"><select id="roleSel">${this.available.filter(r=>!CC.data.roles.includes(r)).map(r=>`<option>${r}</option>`).join('')}</select></div><button class="btn" onclick="Roles.save()">Assign</button>`)},save(){if(!roleSel.value){UI.toast('No additional roles available');return}CC.data.roles.push(roleSel.value);CC.save();UI.close();App.render()},remove(i){CC.data.roles.splice(i,1);CC.save();App.render()}};
