function roles(){return `<div class="page-head"><div><h2>Role Management</h2><p class="muted">Platform Owner is intentionally excluded from institution roles.</p></div><button class="btn primary" id="assign-role">Assign role</button></div><div class="grid two">${state.roles.map((r,i)=>`<div class="card"><div class="avatar">🛡️</div><h3>${r}</h3><p class="muted">Scoped institutional permission</p><span class="tag">Active</span></div>`).join('')}</div>`}function bindRoles(){(document.getElementById('assign-role')||{}).onclick=()=>modal('Assign institution role',`<div class="form"><label>User ID<input class="input"></label><label>Role<select class="input">${state.roles.map(r=>`<option>${r}</option>`).join('')}</select></label><label>Scope<select class="input"><option>Institution</option><option>Department</option><option>Semester / Section</option></select></label><button class="btn primary" data-close>Assign</button></div>`,()=>toast('Role assignment UI ready for secured Firestore write'))}

window.roles=roles;
window.bindRoles=bindRoles;
