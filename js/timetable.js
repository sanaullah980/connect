window.Timetable={day:'Monday',render(){let days=['Monday','Tuesday','Wednesday','Thursday','Friday'];let rows=CC.data.timetable.filter(x=>x.day===this.day);return `<section class="page"><div class="row"><div><h1 class="page-title">Timetable</h1><p class="page-sub">Your weekly academic schedule.</p></div><button class="btn" onclick="Timetable.add()">+ Add class</button></div><div class="day-tabs">${days.map(d=>`<button class="${d===this.day?'active':''}" onclick="Timetable.day='${d}';App.render()">${d}</button>`).join('')}</div><div class="card"><div class="list">${rows.length?rows.map((x,i)=>`<div class="item class"><div class="row"><div><b>${UI.esc(x.subject)}</b><div class="small">${x.time} · ${x.room}</div></div><button class="btn secondary" onclick="Timetable.edit(${CC.data.timetable.indexOf(x)})">Edit</button></div></div>`).join(''):UI.empty('No classes for this day')}</div></div></section>`},add(){this.form()},edit(i){this.form(i)},form(i){let x=i==null?{day:this.day,time:'',subject:'',room:''}:CC.data.timetable[i];UI.modal(i==null?'Add class':'Edit class',`<form onsubmit="Timetable.save(event,${i==null?'null':i})"><div class="field"><label>Day</label><select id="cday">${['Monday','Tuesday','Wednesday','Thursday','Friday'].map(d=>`<option ${x.day===d?'selected':''}>${d}</option>`).join('')}</select></div><div class="field"><label>Subject</label><input id="csub" required value="${UI.esc(x.subject)}"></div><div class="field"><label>Time</label><input id="ctime" required value="${UI.esc(x.time)}" placeholder="09:00 - 10:30"></div><div class="field"><label>Room</label><input id="croom" required value="${UI.esc(x.room)}"></div><button class="btn">Save class</button>${i!=null?` <button type="button" class="btn danger" onclick="Timetable.del(${i})">Delete</button>`:''}</form>`)},save(e,i){e.preventDefault();let x={day:cday.value,subject:csub.value,time:ctime.value,room:croom.value};if(i==null)CC.data.timetable.push(x);else CC.data.timetable[i]=x;CC.save();UI.close();UI.toast('Timetable saved');App.render()},del(i){CC.data.timetable.splice(i,1);CC.save();UI.close();App.render()}};
